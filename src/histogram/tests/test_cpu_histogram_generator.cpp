#include <gmock/gmock.h>

TEST(CPUHistogramGeneratorTest, CorrectOutput) {
    //read in testData.txt
    //CPUHistGenerator histGen(test data vector));
    //frequencies = histGen.getFrequencies();
    //ASSERT_EQ(frequencies, expected frequencies in testFrequencies.txt)
}
